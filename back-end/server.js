

// This is code to get imageurls from the firebase imagehosting
    // const storageRef = firebaseApp.storage().ref();
    // const emotionImages = storageRef.child('emotions/angry2.jpg');
    // console.log(emotionImages.getDownloadURL());


// This is code to access the database
		//     const db = firebaseApp.firestore();
    // 	   const emotions = db.collection("imageURL").doc("emotions");   

// Get all the angry pictures for example:
		//  var query = emotions.get()
    //   .then(snapshot => {
    //     snapshot.forEach(doc => {
    //       console.log(doc.id, '=>', doc.data());
    //     });
    //   }).catch( err => {
    //     console.log('error' , err);
    //   })